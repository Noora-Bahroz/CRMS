# Documentation

- `architecture/` — system design, data flow, module responsibilities.
- `database/` — schema, model relationships, migrations.
- `api/` — future JSON API contracts (auth, cars, bookings, payments).
- `deployment/` — environment setup, WSGI configuration, hosting notes.
- `business-rules/` — pricing, availability, discount, and lifecycle rules.

Placeholders only; documents are written alongside feature implementation.