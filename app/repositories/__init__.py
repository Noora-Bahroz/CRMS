"""Repository package.

Repositories encapsulate database access patterns. Created only where they
provide genuine value (queued here as the thin data-access layer). Service
implementations begin in the feature phase.
"""