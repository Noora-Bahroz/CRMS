"""Test suite package.

Layout:
  tests/conftest.py   shared fixtures
  tests/unit/         pure logic tests (pricing, validation, services)
  tests/integration/  Flask app + database tests (routes, auth, flows)
"""